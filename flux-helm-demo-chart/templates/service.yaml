apiVersion: v1
kind: Service
metadata:
  name: {{ include "flux-helm-demo-chart.fullname" . }}
  labels:
    {{- include "flux-helm-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flux-helm-demo-chart.selectorLabels" . | nindent 4 }}
